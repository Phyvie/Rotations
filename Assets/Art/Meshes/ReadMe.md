!!! Many of these meshes should not be stored here, but rather in MathVisPackage !!!  
The meshes stored here should be exclusively the ones required for specific rotation types; e.g. the Euler gimbal. However meshes like the vector or torus should actually be in MathVis, since they are reusable. 
